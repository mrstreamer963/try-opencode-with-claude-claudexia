{
  "name": "idle-colony-sim",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:wasm && vue-tsc -b && vite build",
    "build:wasm": "wasm-pack build game-core --target web --out-dir ../src/wasm",
    "preview": "vite preview"
  },
  "dependencies": {
    "pixi.js": "^8.19.0",
    "vite-plugin-wasm": "^3.6.0",
    "vue": "^3.5.38"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "@vitejs/plugin-vue": "^6.0.7",
    "@vue/tsconfig": "^0.9.1",
    "typescript": "~6.0.2",
    "vite": "^8.1.0",
    "vue-tsc": "^3.3.5"
  }
}
